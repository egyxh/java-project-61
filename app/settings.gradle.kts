rootProject.name = "java-project-61"

pluginManagement {
    plugins {
        id("org.gradle.kotlin.kotlin-dsl") version "2.1.7"
    }
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
